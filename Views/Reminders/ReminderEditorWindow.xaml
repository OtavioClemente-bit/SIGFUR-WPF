<Window x:Class="SIGFUR.Wpf.Views.Reminders.ReminderEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tarefa prioritária" Width="760" Height="720" MinWidth="640" MinHeight="600"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource AppBackgroundBrush}" Icon="/Assets/sigfur.ico"
        PreviewKeyDown="Window_PreviewKeyDown" Tag="UiState.BoundsOnly">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Border Background="{DynamicResource PrimaryDarkBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="NOVA TAREFA" Foreground="White" FontWeight="Bold" FontSize="19"/>
                <TextBlock Text="Registre a providência, defina sua posição na fila e sinalize somente o que exige atenção imediata."
                           Foreground="{DynamicResource TextOnPrimaryMutedBrush}" Margin="0,5,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/></Grid.ColumnDefinitions>

                    <StackPanel Grid.ColumnSpan="3">
                        <TextBlock Text="O que precisa ser feito" Style="{StaticResource CaptionTextStyle}"/>
                        <TextBox x:Name="ReminderTitleBox" FontSize="16" Padding="10,8"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,16,0,0">
                        <TextBlock Text="Posição na ordem" Style="{StaticResource CaptionTextStyle}"/>
                        <TextBox x:Name="OrderPositionBox" TextAlignment="Center" ToolTip="Use 1 para colocar no topo. Deixe vazio para entrar no fim da fila."/>
                        <TextBlock Text="1 coloca no topo; vazio envia ao final" Style="{StaticResource CaptionTextStyle}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource DangerSoftBrush}" BorderBrush="{DynamicResource DangerBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,16,0,0">
                        <StackPanel>
                            <CheckBox x:Name="UrgentBox" Content="Exige atenção urgente" FontWeight="SemiBold"/>
                            <TextBlock Text="Itens urgentes aparecem no aviso ao abrir o SIGFUR." Style="{StaticResource CaptionTextStyle}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Row="2" Margin="0,16,0,0">
                        <TextBlock Text="Prazo" Style="{StaticResource CaptionTextStyle}"/>
                        <DatePicker x:Name="DueDatePicker" SelectedDateFormat="Short"/>
                        <CheckBox x:Name="NoDateBox" Content="Sem data definida" Margin="0,8,0,0" Checked="NoDateBox_Changed" Unchecked="NoDateBox_Changed"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,16,0,0">
                        <TextBlock Text="Classificação" Style="{StaticResource CaptionTextStyle}"/>
                        <ComboBox x:Name="PriorityBox"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Margin="0,16,0,0">
                        <TextBlock Text="Recorrência" Style="{StaticResource CaptionTextStyle}"/>
                        <ComboBox x:Name="RecurrenceBox"/>
                    </StackPanel>
                    <Border Grid.Row="3" Grid.Column="2" Background="{DynamicResource SurfaceAltBrush}" CornerRadius="10" Padding="12" Margin="0,16,0,0">
                        <CheckBox x:Name="AutoRescheduleBox" Content="Reagendar ao concluir" IsChecked="True" VerticalAlignment="Center"/>
                    </Border>

                    <CheckBox Grid.Row="4" Grid.ColumnSpan="3" x:Name="CompletedBox" Content="Marcar esta tarefa como concluída" Margin="0,16,0,0"/>
                    <StackPanel Grid.Row="5" Grid.ColumnSpan="3" Margin="0,16,0,0">
                        <TextBlock Text="Anotações, etapas ou informações importantes" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
                        <TextBox x:Name="BodyBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="210"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="ValidationText" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancelar" Style="{StaticResource SecondaryButtonStyle}" IsCancel="True" Margin="0,0,8,0"/>
                <Button Content="Salvar tarefa" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
