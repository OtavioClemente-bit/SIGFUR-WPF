<Window x:Class="SIGFUR.Wpf.Views.Licensed.LicensedCopyFormatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copiar formato — Licenciados/Transferidos" Width="860" Height="650" MinWidth="700" MinHeight="520"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource AppBackgroundBrush}" Icon="/Assets/sigfur.ico">
    <Grid Margin="18">
        <Border Style="{StaticResource CardStyle}" Padding="22">
            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel><TextBlock Text="Copiar formato" Style="{StaticResource PageTitleStyle}" FontSize="23"/><TextBlock Text="Monte um modelo reutilizável. A prévia usa todos os militares selecionados." Style="{StaticResource SubtleTextStyle}" Margin="0,3,0,14"/></StackPanel>
                <Border Grid.Row="1" Style="{StaticResource SoftCardStyle}" Padding="12" Margin="0,0,0,12"><TextBlock TextWrapping="Wrap" FontSize="12"><Run Text="Chaves: "/><Run FontWeight="Bold" Text="{}{posto} {nome} {nome_upper} {nome_guerra} {cpf} {cpf_limpo} {prec_cp} {idt} {banco} {agencia} {conta} {endereco} {cep} {ano_formacao} {data_nascimento} {data_praca} {pnr} {motivo} {destino} {telefone} {email} {escolaridade}"/></TextBlock></Border>
                <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Text="Modelo" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,6"/><TextBox x:Name="TemplateBox" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" TextChanged="TemplateBox_TextChanged"/></StackPanel>
                    <StackPanel Grid.Column="2"><TextBlock Text="Prévia" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,6"/><TextBox x:Name="PreviewBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/></StackPanel>
                </Grid>
                <Grid Grid.Row="3" Margin="0,16,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Style="{StaticResource SubtleTextStyle}"/>
                    <Button Grid.Column="1" Content="Restaurar padrão" Style="{StaticResource GhostButtonStyle}" Click="Reset_Click"/>
                    <Button Grid.Column="3" Content="Salvar modelo" Style="{StaticResource SecondaryButtonStyle}" Click="Save_Click"/>
                    <Button Grid.Column="5" Content="Copiar resultado" Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" Click="Copy_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
