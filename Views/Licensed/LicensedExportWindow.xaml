<Window x:Class="SIGFUR.Wpf.Views.Licensed.LicensedExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exportar Licenciados / Transferidos" Width="760" Height="720" MinWidth="650" MinHeight="560"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource AppBackgroundBrush}" Icon="/Assets/sigfur.ico">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Style="{StaticResource CardStyle}" Padding="18" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="EXPORTAÇÃO PROFISSIONAL" Style="{StaticResource SectionTitleStyle}" FontSize="18"/>
                <TextBlock Text="Escolha o formato, o alcance e exatamente quais informações entrarão na tabela." Style="{StaticResource CaptionTextStyle}" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource CardStyle}" Padding="16">
                <StackPanel>
                    <TextBlock Text="Formato" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,8"/>
                    <RadioButton x:Name="ExcelBox" Content="Excel (.xlsx) — cabeçalho, filtro, linhas alternadas e nome de guerra em negrito" IsChecked="True" Margin="0,4"/>
                    <RadioButton x:Name="CsvBox" Content="CSV UTF-8 (.csv) — compatível com Excel e outros sistemas" Margin="0,4"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="16">
                <StackPanel>
                    <TextBlock Text="Registros" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,8"/>
                    <RadioButton x:Name="DisplayedBox" Content="Todos os registros exibidos" IsChecked="True" Margin="0,4"/>
                    <RadioButton x:Name="SelectedBox" Content="Somente os selecionados" Margin="0,4"/>
                    <TextBlock x:Name="ScopeInfoText" Style="{StaticResource CaptionTextStyle}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14">
            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid Margin="0,0,0,10"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Text="Colunas da planilha" Style="{StaticResource SectionTitleStyle}"/><TextBlock Text="A ordem abaixo será a mesma da exportação." Style="{StaticResource CaptionTextStyle}"/></StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Padrão" Style="{StaticResource SecondaryButtonStyle}" Click="Default_Click" Margin="0,0,6,0"/>
                        <Button Content="Todas" Style="{StaticResource SecondaryButtonStyle}" Click="All_Click" Margin="0,0,6,0"/>
                        <Button Content="Nenhuma" Style="{StaticResource SecondaryButtonStyle}" Click="None_Click"/>
                    </StackPanel>
                </Grid>
                <ListBox Grid.Row="1" ItemsSource="{Binding Columns}" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                    <ListBox.ItemTemplate><DataTemplate><CheckBox Content="{Binding Header}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="5,7"/></DataTemplate></ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="ValidationText" VerticalAlignment="Center" Foreground="{DynamicResource DangerBrush}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancelar" Style="{StaticResource SecondaryButtonStyle}" IsCancel="True" Margin="0,0,8,0"/>
                <Button Content="Continuar exportação" Style="{StaticResource PrimaryButtonStyle}" Click="Confirm_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
