<Window x:Class="SIGFUR.Wpf.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SIGFUR" Icon="/SIGFUR;component/Assets/sigfur-loading.ico"
        Width="900" Height="560" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True" Topmost="True">
    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard><Storyboard>
                <DoubleAnimation Storyboard.TargetName="SpinnerRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1.25" RepeatBehavior="Forever"/>
                <DoubleAnimation Storyboard.TargetName="PulseGlow" Storyboard.TargetProperty="Opacity" From="0.18" To="0.55" Duration="0:0:0.9" AutoReverse="True" RepeatBehavior="Forever"/>
            </Storyboard></BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Grid>
        <Border x:Name="RootCard" Margin="18" CornerRadius="20" Background="{DynamicResource AppBackgroundBrush}"
                BorderBrush="{DynamicResource BorderStrongBrush}" BorderThickness="1">
            <Border.RenderTransform><ScaleTransform x:Name="CardScale" ScaleX="0.97" ScaleY="0.97" CenterX="450" CenterY="280"/></Border.RenderTransform>
            <Border.Effect><DropShadowEffect BlurRadius="36" ShadowDepth="9" Opacity="0.28" Color="{DynamicResource ShadowColor}"/></Border.Effect>
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="112"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Border Background="{DynamicResource PrimaryDarkBrush}" CornerRadius="19,19,0,0" Padding="30,20">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <Border Width="64" Height="64" CornerRadius="16" Background="#22FFFFFF" Margin="0,0,18,0">
                            <Image Source="/SIGFUR;component/Assets/sigfur-mark.png" Width="52" Height="52" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Preparando o SIGFUR" Foreground="White" FontSize="26" FontWeight="SemiBold"/>
                            <TextBlock Text="Carregando seu ambiente de trabalho com segurança." Foreground="#D7E9FF" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="SIGFUR" Foreground="#BFD8F4" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                            <TextBlock x:Name="VersionText" Text="v6.2.1" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="0,0,19,19" Padding="34,28">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="72"/></Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock x:Name="StageText" Text="Iniciando componentes…" FontSize="21" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock x:Name="DetailText" Text="Validando recursos e preferências" Style="{StaticResource PageSubtitleStyle}" Margin="0,5,18,0"/>
                            </StackPanel>
                            <Grid Grid.Column="1" Width="58" Height="58">
                                <Ellipse x:Name="PulseGlow" Fill="{DynamicResource PrimarySoftBrush}" Opacity="0.4"/>
                                <Ellipse Width="42" Height="42" Stroke="{DynamicResource DividerBrush}" StrokeThickness="4"/>
                                <Ellipse Width="42" Height="42" Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="4" StrokeDashArray="2,4" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                    <Ellipse.RenderTransform><RotateTransform x:Name="SpinnerRotate" CenterX="21" CenterY="21"/></Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse Width="9" Height="9" Fill="{DynamicResource PrimaryBrush}"/>
                            </Grid>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,24,0,7">
                            <TextBlock Text="PROGRESSO DA INICIALIZAÇÃO" Foreground="{DynamicResource MutedBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock x:Name="PercentText" Text="00%" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryDarkBrush}" FontSize="18" FontWeight="SemiBold"/>
                        </Grid>
                        <ProgressBar x:Name="Progress" Grid.Row="2" Minimum="0" Maximum="100" Value="0" Height="9"/>
                        <ItemsControl x:Name="StepsControl" Grid.Row="3" Margin="0,24,0,10">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate><DataTemplate>
                                <StackPanel Margin="0,0,12,0">
                                    <Border Width="28" Height="28" CornerRadius="14" Background="{Binding Background}" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Marker}" Foreground="{Binding Color}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" Margin="0,7,0,0"/>
                                    <TextBlock Text="{Binding Subtitle}" Foreground="{DynamicResource SubtleTextBrush}" FontSize="9.5" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Border Grid.Row="4" Background="{DynamicResource PrimarySurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="9" Padding="11,8">
                            <TextBlock x:Name="FooterText" Text="Aguarde enquanto o SIGFUR conclui a abertura." Foreground="{DynamicResource MutedBrush}" FontSize="10" TextWrapping="Wrap"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
