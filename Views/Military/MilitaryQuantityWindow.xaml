<Window x:Class="SIGFUR.Wpf.Views.Military.MilitaryQuantityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SIGFUR — Quantidade de militares" Width="720" Height="650" MinWidth="600" MinHeight="520"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResize" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" Icon="/Assets/sigfur.ico">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="84"/><RowDefinition Height="*"/><RowDefinition Height="62"/></Grid.RowDefinitions>
        <Border Background="{DynamicResource PrimaryBrush}" Padding="20,0">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="QUANTIDADE E RESUMO" Foreground="White" FontWeight="Bold" FontSize="19"/>
                <TextBlock x:Name="ScopeText" Foreground="#D7E9FF" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1" Margin="18">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <UniformGrid Columns="4">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0"><StackPanel><TextBlock Text="TOTAL" Style="{StaticResource MetricCaptionStyle}"/><TextBlock x:Name="TotalText" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryDarkBrush}"/></StackPanel></Border>
                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0"><StackPanel><TextBlock Text="RECEBEM AT" Style="{StaticResource MetricCaptionStyle}"/><TextBlock x:Name="WithAtText" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/></StackPanel></Border>
                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0"><StackPanel><TextBlock Text="NÃO RECEBEM AT" Style="{StaticResource MetricCaptionStyle}"/><TextBlock x:Name="WithoutAtText" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}"/></StackPanel></Border>
                <Border Style="{StaticResource CardStyle}"><StackPanel><TextBlock Text="FAVORITOS" Style="{StaticResource MetricCaptionStyle}"/><TextBlock x:Name="FavoriteText" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryDarkBrush}"/></StackPanel></Border>
            </UniformGrid>
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
                <DataGrid x:Name="RankGrid" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Posto/Graduação" Binding="{Binding Rank}" Width="*"/>
                        <DataGridTextColumn Header="Quantidade" Binding="{Binding Count}" Width="120"/>
                        <DataGridTextColumn Header="Percentual" Binding="{Binding Percentage}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
        <Border Grid.Row="2" Background="{DynamicResource SurfaceElevatedBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,0">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="O resumo considera exatamente o escopo indicado acima." VerticalAlignment="Center" Foreground="{DynamicResource MutedBrush}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Copiar resumo" Click="Copy_Click" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="Fechar" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
